# Search Aggregator - 环境变量配置模板
# 复制此文件为 .env 并填入实际值
# cp .env.example .env

# JWT 认证密钥（用于生成和验证登录 token）
# 生成命令: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your-jwt-secret-here

# JWT 算法（可选，默认 HS256）
JWT_ALGORITHM=HS256

# JWT 过期时间（可选，默认 24 小时）
JWT_EXPIRE_HOURS=24
